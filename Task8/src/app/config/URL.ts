import {environment} from '../../environments/environment';

export const URL = {
  people: `${environment.API}people/`,
  planets: `${environment.API}planets/`,
  starships: `${environment.API}starships/`
};
